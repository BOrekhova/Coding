Variables

// a variable is a container for a particular set of bits or type of data

include <string> // to work with strings


int main () 
{
    int x;              // for integer; -2^31 <= int <= 2^31
    long long z;        // for integer; -2^63 <= long long <= 2^63
    float y;            // for not integer nums (5.3)
    double q;           // for not integer nums
    char symbol;        // for characters ('t')
    std::string str;    // for strings ("Barbara")
}
